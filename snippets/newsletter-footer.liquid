<div class="shopify-section-modal flex justify-center">
  <div id="customModalNews" class="fixed inset-0 z-[9999] hidden flex items-start justify-start overflow-y-auto bg-modali_gs bg-modali_gsp w-full lg:w-1/2">
    <div class="bg-transparent w-full overflow-hidden mb-10 transform transition-all">
      
      <div class="flex gap-10 justify-between items-start px-4 md:px-10 lg:px-[64px] xl:px-[128px] pt-6 pb-6 md:pb-10 md:pt-20 lg:pt-[96px]">
        <h2 class="heading-sm_gs md:heading-md_gs lg:heading-lg_gs heading-sm_gsp md:heading-md_gsp lg:heading-lg_gsp text-color_gs text-color_gsp">
          {% if form.posted_successfully? %}
            {{ section.settings.title_ty | default: 'Richiesta inviata' }}
          {% else %}
            {{ section.settings.modal-title | default: 'ISCRIVITI ALLA NEWSLETTER' }}
          {% endif %}
        </h2>
        <button id="closeBtnNews" type="button" class="cursor-pointer">
            <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none">
                <path fill-rule="evenodd" clip-rule="evenodd" d="M18.5418 4.32754C18.8347 4.03472 19.3094 4.03468 19.6023 4.32754C19.8951 4.62042 19.8951 5.09521 19.6023 5.38809L12.99 11.9994L19.6023 18.6117C19.8951 18.9046 19.8951 19.3794 19.6023 19.6723C19.3094 19.9652 18.8346 19.9652 18.5418 19.6723L11.9294 13.06L5.45972 19.5307C5.16682 19.8232 4.69195 19.8234 4.39917 19.5307C4.1064 19.2379 4.10665 18.763 4.39917 18.4701L10.8689 11.9994L4.39917 5.53067C4.1064 5.2379 4.10665 4.76305 4.39917 4.47012C4.69207 4.17723 5.16683 4.17723 5.45972 4.47012L11.9294 10.9389L18.5418 4.32754Z" fill="#EBEFF2"/>
            </svg>
        </button>
      </div>

      {% form 'customer', id: 'newsletter_form' %}
        
        {% if form.posted_successfully? %}
          <div class="flex flex-col gap-6 px-4 md:px-10 lg:px-[64px] xl:px-[128px] py-10 text-color_gs text-color_gsp">
            <p class="caption">{{ section.settings.text_ty | default: 'Presto verrai contattato in merito alla richiesta che hai inviato.' }}</p>
            <button type="button" onclick="document.getElementById('closeBtnNews').click()" class="w-max px-8 py-4 close-cta link-lg_gs link-lg_gsp">
              {{ section.settings.close_ty | default: 'chiudi' }}
            </button>
          </div>
          
          <script>
            document.addEventListener('DOMContentLoaded', function() {
              const modal = document.getElementById('customModalNews');
              if(modal) {
                modal.classList.remove('hidden');
                document.body.style.overflow = 'hidden';
              }
            });
          </script>

        {% else %}
          <div id="news-form-box" class="flex flex-col overflow-y-auto overflow-visible lg:px-10 lg:py-10 py-6 px-4 gap-10 text-color_gs text-color_gsp px-4 md:px-10 lg:px-[64px] xl:px-[128px]">
              
              <div class="flex flex-col gap-12">
                  {% if form.errors %}
                    <div class="text-red-500 caption">{{ form.errors | default_errors }}</div>
                  {% endif %}

                  <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                      <div class="md:col-span-2 flex justify-start caption text-color_gs text-color_gsp opacity-70">
                          {{ section.settings.news-form-required-text }}
                      </div>

                      <div class="flex flex-col gap-1">
                          <div class="flex caption">{{ section.settings.news-fname-label}}{% if section.settings.news-fname-required %}*{% endif %}</div>
                          <input class="caption opacity-[50%] py-1 bg-transparent border-b border-white outline-none" type="text" name="contact[first_name]" placeholder="{{ section.settings.news-fname-placeholder }}" {% if section.settings.news-fname-required %}required{% endif %}>
                      </div>

                      <div class="flex flex-col gap-1">
                          <div class="flex caption">{{ section.settings.news-lname-label}}{% if section.settings.news-lname-required %}*{% endif %}</div>
                          <input class="caption opacity-[50%] py-1 bg-transparent border-b border-white outline-none" type="text" name="contact[last_name]" placeholder="{{ section.settings.news-lname-placeholder }}" {% if section.settings.news-lname-required %}required{% endif %}>
                      </div>

                      <div class="flex flex-col gap-1 relative">
                          <div class="flex caption">{{ section.settings.news-country-label}}*</div>
                          <input class="caption country-input-news opacity-[50%] border-b py-1 border-white pr-10 bg-transparent outline-none" type="text" name="contact[country]" autocomplete="off" required placeholder="{{ section.settings.news-form-boat-q-port-placeholder}}">
                          <div class="country-flag-news absolute right-8 bottom-0 pb-2 mr-2 z-50 pointer-events-none"></div>
                          <div class="country-arrow-news absolute right-0 bottom-0 pb-2 mr-2 z-50 pointer-events-none transition-transform duration-300">
                              <svg width="20" height="20" viewBox="0 0 24 24" fill="none"><path d="M4 8L10.5858 14.5858C11.3668 15.3668 12.6332 15.3668 13.4142 14.5858L20 8" stroke="white" stroke-linecap="round"/></svg>
                          </div>
                          <ul class="countryList-news absolute top-full mt-1 border_gs bg-modali_gsp max-h-60 overflow-y-auto w-full rounded-[16px] shadow-md hidden z-50"></ul>
                      </div>

                      <div class="flex flex-col gap-1">
                          <div class="flex caption">{{ section.settings.news-mail-label}}{% if section.settings.news-mail-required %}*{% endif %}</div>
                          <input class="caption opacity-[50%] py-1 bg-transparent border-b border-white outline-none" type="email" name="contact[email]" placeholder="{{ section.settings.news-mail-placeholder }}" {% if section.settings.news-mail-required %}required{% endif %}>
                      </div>
                  </div>

                 <div class="caption text-color_gs text-color_gsp flex flex-col gap-4">
                      <div class="flex">{{ section.settings.news-boat-sel-label }}</div>
                      {% assign options = section.settings.options | split: ',' %}
                      <div class="flex flex-wrap gap-2">
                          {% for option in options %}
                          {% assign opt = option | strip %}
                          <label class="pill-btn-news flex gap-1 py-1.5 px-3 cursor-pointer rounded-full border border-white transition-all">
                              <input type="checkbox" class="multi-checkbox hidden" value="{{ opt }}">
                              {{ opt }}
                          </label>
                          {% endfor %}
                      </div>
                      <input type="hidden" name="contact[boat_selection]" id="boat_selection_hidden">
                  </div>

                  <div class="caption text-color_gs text-color_gsp flex flex-col gap-4">
                      <div class="flex">{{ section.settings.news-boat-q-label }}</div>
                      <div class="flex flex-col gap-2">
                          <div class="flex flex-col gap-2">
                              <div class="flex gap-2 items-center">
                                  <input type="radio" class="radio-toggle size-5 rounded-full border border-white appearance-none checked:bg-white cursor-pointer" id="boat-y" name="contact[boat_question]" value="yes" data-target="boat-y-fields">
                                  <label for="boat-y">{{ section.settings.news-boat-q-affermative-label }}</label>
                              </div>
                              <div id="boat-y-fields" class="hidden flex flex-col md:flex-row gap-6 my-2">
                                  <input class="caption border-b border-white bg-transparent py-1 w-full outline-none" type="text" name="contact[current_model]" placeholder="{{ section.settings.news-form-boat-q-model-placeholder }}">
                                  <input class="caption border-b border-white bg-transparent py-1 w-full outline-none" type="text" name="contact[current_port]" placeholder="{{ section.settings.news-form-boat-q-port-placeholder }}">
                              </div>
                          </div>
                          <div class="flex flex-col gap-2">
                              <div class="flex gap-2 items-center">
                                  <input type="radio" class="radio-toggle size-5 rounded-full border border-white appearance-none checked:bg-white cursor-pointer" id="boat-o" name="contact[boat_question]" value="other" data-target="boat-o-fields">
                                  <label for="boat-o">{{ section.settings.news-boat-q-other-label }}</label>
                              </div>
                              <div id="boat-o-fields" class="hidden flex flex-col md:flex-row gap-4 my-2 w-full">
                                  <input class="caption border-b border-white bg-transparent py-1 w-full outline-none" type="text" name="contact[other_model]" placeholder="{{ section.settings.news-form-boat-q-model-placeholder }}">
                                  <input class="caption border-b border-white bg-transparent py-1 w-full outline-none" type="text" name="contact[other_port]" placeholder="{{ section.settings.news-form-boat-q-port-placeholder }}">
                              </div>
                          </div>
                          <div class="flex gap-2 items-center">
                              <input type="radio" class="radio-toggle size-5 rounded-full border border-white appearance-none checked:bg-white cursor-pointer" id="boat-n" name="contact[boat_question]" value="no">
                              <label for="boat-n">{{ section.settings.news-boat-q-negative-label }}</label>
                          </div>
                      </div>
                  </div>

                  <div class="flex flex-col gap-6">
                      <div class="flex items-center gap-4">
                          <input type="checkbox" class="w-6 h-6 cursor-pointer" id="marketing-consent-news">
                          <label class="caption" for="marketing-consent-news">
                              {{ section.settings.news-form-markConsLabel }}
                              <u><a href="{{ section.settings.news-privacy-url }}">{{ section.settings.news-form-privacy-label | default: "Privacy Policy" }}</a></u>
                          </label>
                      </div>
                      <div class="flex items-center gap-4">
                          <input type="checkbox" class="w-6 h-6 cursor-pointer disabled:opacity-30" id="profiling-consent-news" disabled>
                          <label class="caption transition-opacity duration-200" id="profiling-label-news" for="profiling-consent-news" style="opacity: 0.5;">
                              {{ section.settings.news-form-profConsLabel }}
                              <u><a href="{{ section.settings.news-privacy-url }}">{{ section.settings.news-form-privacy-label | default: "Privacy Policy" }}</a></u>
                          </label>
                      </div>
                  </div>

                  <div class="flex justify-end mt-4">
                      <button id="news-form-submit" type="submit" class="px-8 py-4 bg-[#76787B] text-black rounded-full font-bold transition-all hover:bg-white">
                          {{ section.settings.news-form-submit-button-text }}
                      </button>
                  </div>

                  <input type="hidden" name="contact[tags]" id="CustomerTags" value="newsletter">
              </div>
          </div>
        {% endif %}
      {% endform %}
    </div>
  </div>
</div>

<style>
    .countryList-news::-webkit-scrollbar { display: none; }
    .country-arrow-news.open svg { transform: rotate(180deg); }
    #profiling-consent-news:not(:disabled) + label { opacity: 1 !important; }
</style>

<script>
document.addEventListener('DOMContentLoaded', function () {
    const modal = document.getElementById('customModalNews');
    const openBtn = document.getElementById('openModalNews'); 
    const closeBtn = document.getElementById('closeBtnNews');
    const form = document.getElementById('news_form');

    // MODAL OPEN/CLOSE
    if (openBtn) openBtn.onclick = () => { modal.classList.remove('hidden'); document.body.style.overflow='hidden'; };
    if (closeBtn) closeBtn.onclick = () => { modal.classList.add('hidden'); document.body.style.overflow=''; };

    // RADIO CONDITIONAL FIELDS
    document.querySelectorAll('.radio-toggle').forEach(radio => {
        radio.addEventListener('change', function() {
            // Nasconde tutti i target dello stesso name group
            const groupName = this.getAttribute('name');
            document.querySelectorAll(`input[name="${groupName}"]`).forEach(r => {
                const targetId = r.getAttribute('data-target');
                if (targetId) {
                    const targetEl = document.getElementById(targetId);
                    if (targetEl) targetEl.classList.add('hidden');
                }
            });
            // Mostra il target di quello selezionato
            const activeId = this.getAttribute('data-target');
            if (activeId) {
                const activeEl = document.getElementById(activeId);
                if (activeEl) activeEl.classList.remove('hidden');
            }
        });
    });

    // MULTI CHECKBOX PILLS (FIXED)
    document.querySelectorAll('.pill-btn-news').forEach(pill => {
        pill.addEventListener('click', function(e) {
            const cb = this.querySelector('input');
            cb.checked = !cb.checked;
            this.classList.toggle('selected', cb.checked);
            
            const selected = Array.from(document.querySelectorAll('.multi-checkbox:checked'))
                                 .map(el => el.value);
            document.getElementById('boat_selection_hidden').value = selected.join(', ');
        });
    });

    // TEXTAREA COUNTER
    const tx = document.getElementById('info-message');
    if (tx) {
        tx.oninput = function() {
            this.style.height = 'auto';
            this.style.height = this.scrollHeight + 'px';
            document.getElementById('char-counter').textContent = this.value.length + ' / 250';
        };
    }

    // CONSENT LOGIC
    const mkt = document.getElementById('marketing-consent-news');
    const prof = document.getElementById('profiling-consent-news');
    const profLabel = document.getElementById('profiling-label-news');
    if (mkt && prof) {
        mkt.onchange = function() {
            prof.disabled = !this.checked;
            if (!this.checked) { 
                prof.checked = false; 
                profLabel.style.opacity = "0.5"; 
            } else { 
                profLabel.style.opacity = "1"; 
            }
        };
    }

    // COUNTRY AUTOCOMPLETE
    let countries = [];
    fetch('https://restcountries.com/v3.1/all?fields=name,cca2').then(r => r.json()).then(data => {
        countries = data.map(c => ({ name: c.name.common, code: c.cca2.toLowerCase() })).sort((a,b) => a.name.localeCompare(b.name));
    });

    const cInput = document.querySelector('.country-input-news');
    const cList = document.querySelector('.countryList-news');
    if (cInput) {
        cInput.oninput = function() {
            const val = this.value.toLowerCase();
            cList.innerHTML = '';
            if (val.length < 1) { cList.classList.add('hidden'); return; }
            const matches = countries.filter(c => c.name.toLowerCase().includes(val)).slice(0, 8);
            matches.forEach(c => {
                const li = document.createElement('li');
                li.className = 'px-4 py-2 hover:bg-white hover:text-black cursor-pointer flex items-center gap-2 text-white text-sm';
                li.innerHTML = `<img src="https://flagcdn.com/w20/${c.code}.png" width="20"> <span>${c.name}</span>`;
                li.onmousedown = () => {
                    cInput.value = c.name;
                    document.querySelector('.country-flag-news').innerHTML = `<img src="https://flagcdn.com/w20/${c.code}.png" width="20">`;
                    cList.classList.add('hidden');
                };
                cList.appendChild(li);
            });
            cList.classList.remove('hidden');
        };
        cInput.onblur = () => setTimeout(() => cList.classList.add('hidden'), 200);
    }

    // SUBMIT HANDLER
    if(form) {
      form.onsubmit = function() {
          let tags = ['newsletter'];
          if (mkt && mkt.checked) tags.push('mkt_ok');
          if (prof && prof.checked) tags.push('prof_ok');
          document.getElementById('CustomerTags').value = tags.join(',');
          document.getElementById('news-form-submit').disabled = true;
          document.getElementById('news-form-submit').innerText = '...';
      };
    }
});
</script>